#ifndef EVENTS_CPP
#define EVENTS_CPP

#include "Events.h"

Event::Event(unsigned int type, unsigned int object1, unsigned int object2)
	:type(type), object1(object1), object2(object2)
{

}













#endif